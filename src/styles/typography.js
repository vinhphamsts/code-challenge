import styled from 'styled-components';
import { mainTextColor, lightTextColor } from './colors.js';

export const Heading = styled.h3`
  ${mainTextColor};
`;

export const Title = styled.h4`
  ${lightTextColor};
`;